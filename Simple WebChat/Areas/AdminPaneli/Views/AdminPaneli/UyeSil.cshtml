@model Simple_WebChat.Models.Mdl.Users

@{
    ViewBag.Title = "UyeSil";
    Layout = "~/Areas/AdminPaneli/Views/Shared/_Layout.cshtml";
}
<div class=" content-wrapper">
    <div class="container-fluid">

        <h6>Silmek istediğinize emin misiniz?</h6>
        <div>
            <h4>Kullanıcı:</h4>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.UserName)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.UserName)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Email)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Email)
                </dd>

            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Delete" class="btn btn-default" /> |
                    @Html.ActionLink("Listeye geri dön", "UyeleriListele")
                </div>
            }
            @Html.Raw(ViewBag.mesajSil)
        </div>
        </div>
</div>